{
  "name": "unit-test",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": ">=4.2.1"
  },
  "main": "app/app.js",
  "scripts": {
    "start": "node $npm_package_main",
    "test": "npm run coverage",
    "test-only": "mocha -R spec test/test.spec.js",
    "test-unit": "mocha -R spec test/unit/unit.spec.js",
    "test-integration": "mocha test/integration/integration.spec.js",
    "coverage": "istanbul --include-all-sources cover _mocha -- -R spec --recursive test && codeclimate-test-reporter < coverage/lcov.info",
    "clean": "rm -rf node_modules/ dist/ coverage/ npm-debug.log"
  },
  "author": "Nate Clark <n8@n8io.com>",
  "license": "MIT",
  "dependencies": {
    "async": "^1.4.2",
    "express": "^4.13.3",
    "lodash": "^3.10.1",
    "morgan": "^1.6.1"
  },
  "devDependencies": {
    "chai": "^3.3.0",
    "codeclimate-test-reporter": "^0.1.1",
    "istanbul": "^0.3.22",
    "mocha": "^2.3.3",
    "mocha-istanbul": "^0.2.0",
    "sinon": "^1.17.1",
    "supertest": "^1.1.0"
  }
}
